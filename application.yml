spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/wakeup?serverTimezone=Asia/Seoul}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:q1w2e3}

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:qlalfql123@gmail.com}
    password: ${MAIL_PASSWORD:isym airk kqwa tskx}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: ${JWT_SECRET:K8vX2mN9pQ3rT5tY7Wa1bC4dE6lG0hI!}
  expiration: 86400000
